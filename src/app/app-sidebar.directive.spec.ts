import { AppSidebarDirective } from './app-sidebar.directive';

describe('AppSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
